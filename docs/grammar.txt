PROGRAM ::= ( EXPRESSION ';' )*

EXPRESSION ::= UNARY BINOPRHS

UNARY ::=
    | PRIMARY
    | ('+' | '-') UNARY

BINOPRHS ::= ( ('+' | '-' | '*' | '/') UNARY )*

PRIMARY ::=
    | number
    | '(' EXPRESSION ')'
